{
  "name": "devboost-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DevBoost - Cross-platform embedded development agent",
  "author": "DevBoost Contributors",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/free-revalution/DevBoost.git"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "start": "pnpm --filter @devboost/cli start",
    "start:simple": "pnpm --filter @devboost/cli start:simple",
    "install:global": "pnpm build && cd packages/cli && pnpm link --global",
    "uninstall:global": "cd packages/cli && pnpm unlink --global"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
